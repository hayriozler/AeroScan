namespace WebApp;

/// <summary>
/// Marker class for shared localization resources.
/// Resource files: Resources/SharedResources.resx (EN default), Resources/SharedResources.tr.resx (TR).
/// Inject in components via: @inject IStringLocalizer&lt;SharedResources&gt; L
/// </summary>
public sealed class SharedResources { }
